# 이 파일을 복사해서 이름을 ".env" 로 바꾼 뒤 값을 채우세요.
#   cp env.example.txt .env

# Google AI Studio 에서 발급 (https://aistudio.google.com/apikey)
# 실제 키로 반드시 바꾸세요. 한글이 남아 있으면 분석 단계에서
# "'ascii' codec can't encode characters" 오류가 납니다.
GEMINI_API_KEY=AIzaSy...

# 모델 ID.
# 아래 값은 2026년 7월 기준 실제 호출 가능한 이름으로 확인된 것들입니다.
# 모델 이름 오류(404 NOT_FOUND)가 나면 앱 사이드바의 "사용 가능한 모델 확인"을
# 눌러 내 키로 쓸 수 있는 목록을 보고 그중 하나로 바꾸세요.
#
# RESEARCH: 상품명으로 웹검색(그라운딩)을 도는 단계.
# ANALYSIS: 이미지를 읽고 장단점 후보를 뽑는 단계. 비전 성능이 중요.
# WRITER:   실제 리뷰 문장을 쓰는 단계. 속도/비용 위주로 낮춰도 무방.
RESEARCH_MODEL=gemini-3.5-flash
ANALYSIS_MODEL=gemini-3.6-flash
WRITER_MODEL=gemini-3.6-flash

# 위 셋은 모두 preview 가 아닌 안정화 버전입니다.
# preview 모델은 이름이 바뀌거나 예고 없이 사라져서 404 를 만들기 때문에
# 기본값으로는 쓰지 않습니다.
#
# 참고 - 다른 선택지 (전부 안정화 버전)
#   비용을 더 줄이려면:  gemini-3.5-flash-lite  또는  gemini-3.1-flash-lite
#   구세대 고성능 추론:   gemini-2.5-pro
#   무난한 범용:        gemini-2.5-flash

# 이미지 긴 변을 이 픽셀로 줄여서 전송한다 (토큰 비용 절감)
MAX_IMAGE_EDGE=1280

# 같은 이미지+상품명 조합의 분석 결과를 디스크에 캐싱할지 여부
CACHE_ENABLED=true
